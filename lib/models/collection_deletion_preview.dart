class CollectionDeletionPreview {
  final int totalBooks;
  final int toDelete;
  final int toKeep;

  const CollectionDeletionPreview({
    required this.totalBooks,
    required this.toDelete,
    required this.toKeep,
  });
}
